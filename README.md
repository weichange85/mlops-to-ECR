# Iris Classifier API

A simple ML model trained on the Iris dataset, served via FastAPI.

## 🚀 Usage

1. Train the model:

    python app/train.py


2. Run the API:

    uvicorn main:app --reload


3. Visit:

    http://127.0.0.1:8000/docs ("may defer depending on port")


